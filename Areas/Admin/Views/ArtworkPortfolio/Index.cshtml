@model IEnumerable<ArtworkPortfolio>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<div class="border backgroundWhite">
    <div class="row">
        <div class="col-6">
            <h2 class="text-info"> Artwork Portfolio List</h2>
        </div>
        <div class="col-6 text-right">
            <partial name="_CreateButtonPartial" />
        </div>
    </div>
    <br />
    <div>
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>
                        @Html.DisplayNameFor(m => m.Name)
                    </th>

                    <th>
                        @Html.DisplayNameFor(m => m.Description)
                    </th>

                    <th>
                        @Html.DisplayNameFor(m => m.Artist)
                    </th>

                    <th>
                        @Html.DisplayNameFor(m => m.MediumId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.ArtworkTypeId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.Size)
                    </th>
                    <th>
                        @Html.DisplayNameFor(m => m.Price)
                    </th>
                    <th></th>
                    <th></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(m => item.Name)
                        </td>
                        <td>
                            @Html.Raw(item.Description)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.Artist)
                        </td>

                        <td>
                            @Html.DisplayFor(m => item.Medium.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.ArtworkType.Name)
                        </td>

                        <td>
                            @Html.DisplayFor(m => item.Size)
                        </td>

                        <td>
                            @Html.DisplayFor(m => item.Price)
                        </td>
                        <td>
                            <partial name="_TableButtonPartial" model="item.Id" />
                        </td>
                    </tr>
                }
            </table>
        }
        else
        {
            <p>No Artwork Portfolio exists...</p>
        }
    </div>
</div>
